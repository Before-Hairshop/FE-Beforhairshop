import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function Loading() {
  return (
    <View>
      <Text>Virtual Styling</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
